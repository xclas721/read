# 專案交接說明（給接手的 AI）

> 這是接力說明。使用者在另一台電腦做到一半，git pull 下來繼續。請先讀完這份再動手。
> 最後更新：2026-06-17

---

## 這是什麼

公司內部**教育訓練**課程「Spec-Driven 工程師 — AI Agent 時代的需求釐清訓練」的工作區。
存放課程教材 + 使用者的課前作業產出。

## 人物與稱呼（重要）

- **使用者 = Kyson**，職稱是**工程師**。
- **Ardi（莊惠迪，ardichuang@hitrust.com）= Kyson 的主管**，負責主辦這次訓練。
- ⚠️ 這是公司教育訓練、不是學校。**稱呼 Ardi 為「主管」，不要叫「老師」。**

## 下一個死線

**2026/6/18（四）14:00** W3 課堂（7 樓巴厘島會議室）。課前作業要在這之前寄給 Ardi。

---

## 目錄結構

| 資料夾 | 內容 | 可否修改 |
|---|---|---|
| `01_自學模組/` | 主管給的自學教材 M1–M6（pdf/md） | ❌ **唯讀，別改** |
| `02_課堂投影片與圖片/` | 主管給的投影片、slido 截圖 | ❌ **唯讀，別改** |
| `03_範本/` | 主管給的 spec-template、w3-需求清單-template | ❌ **唯讀，別改** |
| `04_課堂產出/` | 全班訪談紀錄、三組衝突紀錄（原料） | ❌ **唯讀，別改** |
| `05_我的作業/` | Kyson 的產出（需求清單、spec 草稿） | ✅ 可改 |
| `06_待辦/` | 待辦清單 | ✅ 可改 |

**鐵則：01–04 是主管提供的材料，只能讀、移動位置，絕不可編輯內容。** 已驗證目前內容零改動。

---

## 進度狀態

### 已完成
- 解析主管來信、釐清作業要求
- 目錄分層整理、刪除廢檔（.eml/.ics/空白 .htm）
- `05_我的作業/kyson_w3-需求清單.md` — 依 template 綜整三組衝突的需求詳細清單
- `05_我的作業/kyson_w3-spec.md` — W3 spec 草稿（12 條 Must 全附 Given/When/Then 驗收 + Traceability）

### ⚠️ 釐清過的關鍵誤會（別重蹈）
- **課前唯一必交 = 「需求詳細清單」**，不是 spec。
- **spec 是 W3 當天課堂才寫的**（spec 草稿是超前準備的 bonus，非必交）。
- 自學包 M4 / M5 **要 Kyson 本人讀**——AI 讀過不算數。

### 待辦（只有 Kyson 能做）
- [ ] 把 `05_我的作業/kyson_w3-需求清單.md` 寄給 Ardi（6/18 前）
- [ ] 本人讀 `01_自學模組/m4-design.md`、`m5-sdd.md`
- 詳見 `06_待辦/待辦清單.md`

### 待 Kyson 確認後 AI 才能往下
- **規模數字**：spec 用「客戶約 300 筆」，來自 A 組簡報、**尚未經 Kyson 確認**。若不對，效能段與 NFR 要改。
- **聯絡人上限**：目前設「無上限」（依 Ray 訪談），是否加防濫用上限未定。
- Should 需求（R14/R15/R17/R18）的驗收條件尚未補。

### 卡在開放問題（待主管/資安拍板，非 Kyson 一人可決，spec 內已標 TBD）
- domain 自動歸戶 vs CC 人工防詐騙（資安取捨）
- 部署平台 AWS vs GCP
- ISO-27001 導入時程與 approve 範圍
- 服務常駐 vs 冷啟動 SLA 秒數（影響 spec 的 T12.1 驗收）

---

## 環境注意：技能不會跟著 git pull

上一台電腦裝了兩個 user 層級技能（在 `~/.claude/skills/`，**不在 repo 內**，pull 不會帶過來）：
- **find-skills** — 搜尋/安裝技能
- **humanizer-tw** — 繁中去 AI 味（曾用來檢查作業，結論：文件已乾淨無需大改）

若需要同樣能力，在公司電腦自行安裝：
```
npx skills add yelban/humanizer.tw -g -y
```
注意：spec 是技術文件，用 humanizer 時**只做不改語意的微潤**（刪贅詞、拆「的」字堆疊），別套用它的「注入個性/口語化」那套，會破壞 spec 精確度。

---

## 工作慣例

- commit 訊息用繁中、結尾加 `Co-Authored-By: Claude ...` trailer（沿用現有風格）。
- commit 前先 `git status` / `git diff` 確認沒動到 01–04 的主管材料。
- 直接 commit 到 `main`（這是個人筆記 repo，沒有 PR 流程）。
